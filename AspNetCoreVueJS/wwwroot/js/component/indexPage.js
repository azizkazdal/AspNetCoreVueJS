import Vue from "../../lib/vue/vue.esm.browser.js";

const indexPage = Vue.component('index-page', {
    data() {
        return {
            indexpageContent: 'Application Index Page Content'
        }
    },
});