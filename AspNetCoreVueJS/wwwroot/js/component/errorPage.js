import Vue from "../../lib/vue/vue.esm.browser.js";

const errorPage = Vue.component('error-page', {
    data() {
        return {
            errorPageContent :'VueJS Error page content'
        }
    }
});