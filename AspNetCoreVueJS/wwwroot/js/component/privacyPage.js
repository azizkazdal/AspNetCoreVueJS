import Vue from "../../lib/vue/vue.esm.browser.js";

const privacyPage = Vue.component('privacy-page', {
    data() {
        return {
            privacyPageContent :'Privacy page content coming from VueJS component'
        }
    }
});